import { ESearchMode } from "@/types/ui.types";

export const SearchInputPlaceholder = {
  [ESearchMode.Read]: "Read with your own narrative...",
  [ESearchMode.Search]: "Search, Ask, Explore...",
};
