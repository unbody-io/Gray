export const fields = [
    "tagName",
    "html",
    "order",
    "document.GoogleDoc.remoteId",
    "document.GoogleDoc.slug",
    "document.GoogleDoc.pathString",
    "__typename",
    "classNames",
] as const;
